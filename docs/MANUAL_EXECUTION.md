# Manual execution

Manual mode creates proposals. Acceptance changes local state only and always displays: **Manual execution only. No order has been transmitted.** The user records entry and exit fills; realized P&L is calculated from those fills and fees. No browser or external order automation exists.

