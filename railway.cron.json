{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE"
  },
  "deploy": {
    "startCommand": "python3 scripts/refresh_and_evaluate.py --storage database",
    "restartPolicyType": "NEVER"
  }
}
